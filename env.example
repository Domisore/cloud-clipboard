# Cloudflare R2 Configuration
# Get these values from your Cloudflare dashboard:
# 1. Go to R2 in your Cloudflare dashboard
# 2. Create a bucket (if you haven't already)
# 3. Go to "Manage R2 API Tokens" to create credentials

R2_ACCOUNT_ID=your_cloudflare_account_id
R2_ACCESS_KEY_ID=your_r2_access_key_id
R2_SECRET_ACCESS_KEY=your_r2_secret_access_key
R2_BUCKET_NAME=your_bucket_name

# Upstash Redis Configuration (for metadata storage)
# Get these from https://console.upstash.com/
UPSTASH_REDIS_REST_URL=your_upstash_redis_rest_url
UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_rest_token
