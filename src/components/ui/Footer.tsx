export function Footer() {
    return (
        <footer className="fixed bottom-0 left-0 right-0 h-8 border-t border-border-color bg-background flex items-center justify-center text-xs text-gray-500 z-50">
            <p>TOSSED VIA DRIVE.IO // NO LOGS // NO MASTERS</p>
        </footer>
    );
}
